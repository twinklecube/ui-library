import { Colors } from "../../utils";
interface IconWrapperProps {
    size?: number;
    color?: Colors;
    hoverColor?: Colors;
}
export declare const IconWrapper: import("styled-components").StyledComponent<"svg", any, IconWrapperProps, never>;
export {};
